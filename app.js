const express=require("express");const path=require("path");const {port}=require("./config/config");const routes=require("./router");const app=express();app.set("view engine","ejs");app.set("views",path.join(__dirname,"views"));app.use("/assets",express.static(path.join(__dirname,"..","public")));app.use(express.urlencoded({extended:true}));app.use(express.json());app.use("/",routes);app.use((req,res)=>res.status(404).render("404",{title:"Page Not Found",page:"Not Found"}));app.listen(port,()=>console.log(`Server running at http://localhost:${port}`));